# ARGOS Isaac Lab 학습 환경 Python 의존성
# Isaac Sim 컨테이너 내 pip install -r requirements-training.txt

# 강화학습 환경 인터페이스
gymnasium>=0.29.0

# 단일 에이전트 학습 (Level 1~2 커리큘럼)
stable-baselines3>=2.3.0

# 멀티에이전트 학습 (Level 3 커리큘럼)
# ray[rllib]은 용량이 크므로 필요 시 주석 해제
# ray[rllib]>=2.10.0

# 수치 연산 (Isaac Sim 내 NumPy는 이미 포함, 버전 확인용)
numpy>=1.24.0

# 학습 시각화
tensorboard>=2.14.0

# 설정 파일 (학습 하이퍼파라미터 YAML 관리)
omegaconf>=2.3.0

# 진행 상황 표시
tqdm>=4.66.0
